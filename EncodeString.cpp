#include <string>
#include <vector>

using namespace std;

// This should be using dynamic programming/memo. The shortest encoding string en[i..j]
// is the sub-problem, in which the encoding result could be one of the following:
// origin_string |  k[str] | multiparts

// Example inputs:
// kaabcaabcd => k[aabc]2d

class Solution {
public:
    string encode(string s) {
        
    }
};